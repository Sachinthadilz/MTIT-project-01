/**
 * Shared application constants.
 * Values mirror backend validation limits so they stay in sync.
 */

/** Maximum character length for a note title (matches backend) */
export const NOTE_MAX_TITLE = 200;

/** Maximum character length for note content (matches backend) */
export const NOTE_MAX_CONTENT = 10_000;
